import {SidebarType, StoreActionType} from "./store";

export const sidebarReducer = (state: SidebarType, action: StoreActionType): SidebarType => {
    switch (action.type) {

        default:
            return state
    }
}